"""Tool modules. Each registers against the one server in ``fpl_edge.mcp.server``.

Empty on purpose: importing this package must not import twelve modules and
the panels behind them. ``server.py`` names each module explicitly.
"""
